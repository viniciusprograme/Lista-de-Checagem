MinIO local storage included for S3-compatible testing.
Access console at http://localhost:9001
Default credentials are set in `.env` (MINIO_ROOT_USER, MINIO_ROOT_PASSWORD)
Create a bucket named `checklist` and use it for object storage.
