Grafana provisioned with Prometheus datasource. Default admin credential: `admin`/`admin` after first start (change on first login).
Place dashboards under `infra/monitoring/grafana/dashboards` to be picked up by provisioning.
